<script language="javascript">
var token = document.location.hash.substring(1);

var deliverToken = function(token) {
	alert('you are not running this in an iframe!');
};

if (window.top.deliverToken)
	window.top.deliverToken(token);
</script>